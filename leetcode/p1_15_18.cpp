/**
 * 这三个问题在数组中找指定个数的元素和为target的问题
 * 可以for循环遍历查找，也可以建hash表辅助查找
 */
#include <hash_set>

 int** twoSum(int* arr, int size, int target);
 int** threeSum(int* arr, int size, int target);
 int** fourSum(int* arr, int size, int target);





